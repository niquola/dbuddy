# Database Configuration
# Standard PostgreSQL environment variables
PGHOST=localhost
PGPORT=15432
PGDATABASE=dbuddy_dev
PGUSER=postgres
PGPASSWORD=your_password_here

# Alternative naming convention (optional)
# DATABASE_HOST=localhost
# DATABASE_PORT=15432
# DATABASE_NAME=dbuddy_dev
# DATABASE_USER=postgres
# DATABASE_PASSWORD=your_password_here

# Connection string (alternative to individual variables)
# DATABASE_URL=postgresql://username:password@localhost:15432/database_name 